diff -ruN dbug-orig/src/scripts/explorer.rb dbug-modified/src/scripts/explorer.rb
--- dbug-orig/src/scripts/explorer.rb	2012-12-27 14:22:33.359083360 -0500
+++ dbug-modified/src/scripts/explorer.rb	2013-01-16 22:27:57.816212866 -0500
@@ -5,6 +5,7 @@
 require 'set'
 require 'socket'
 require 'tree'
+require 'fileutils'
 
 # Class for events
 class Event
@@ -390,10 +391,10 @@
 end
 
 # Check for existence of the program to be tested
-if (not File.exist?("#{ARGV[0]}"))
-  puts "[EXPLORER] The program to be tested does not exist"
-  exit(-1)
-end
+#if (not File.exist?("#{ARGV[0]}"))
+#  puts "[EXPLORER] The program to be tested does not exist"
+#  exit(-1)
+#end
 
 # Repeat until you have covered all choices (equivalence classes) of
 # the decision tree
@@ -429,11 +430,11 @@
     client.close
     if client_pid == arbiter_child.to_s then break end
   end
-  File.rename("strategy", "#{$logs}/strategy-#{iter}")
+  FileUtils.move("strategy", "#{$logs}/strategy-#{iter}")
 
   # Start the test
   ENV[ 'LD_PRELOAD' ] = $ilayer
-  system(*ARGV)
+  system(*(ARGV[0]))
   ENV[ 'LD_PRELOAD' ] = nil
 
   # Wait for the arbiter to finish
@@ -445,13 +446,13 @@
   process_log(tree,"#{$output_dir}/dbug-history")
 
   # Log information generated by this iteration
-  File.rename("#{$output_dir}/dbug-history",
+  FileUtils.move("#{$output_dir}/dbug-history",
   "#{$logs}/dbug-history-#{iter}")
-  File.rename("#{$output_dir}/dbug-server",
+  FileUtils.move("#{$output_dir}/dbug-server",
   "#{$logs}/dbug-server-#{iter}")
-  File.rename("#{$output_dir}/dbug-interposition",
+  FileUtils.move("#{$output_dir}/dbug-interposition",
   "#{$logs}/dbug-interposition-#{iter}")
-  File.rename("#{$output_dir}/dbug-client",
+  FileUtils.move("#{$output_dir}/dbug-client",
   "#{$logs}/dbug-client-#{iter}")
 end
 
